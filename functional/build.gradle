dependencies {
   compile project(':main')
   testCompile project(':builders')
}

sourceSets {
   test {
      java {
         srcDir 'java'
      }
      resources {
         srcDir 'resources'
      }
   }
}
