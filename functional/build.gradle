dependencies {
   compile project(':main')
   compile project(':builders')
}

sourceSets {
   test {
      java {
         srcDir 'java'
      }
      resources {
         srcDir 'resources'
      }
   }
}
