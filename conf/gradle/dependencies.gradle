// vim: ft=groovy

def jettyVersion = "8.1.7.v20120910"
   def snakeYamlVersion = "org.yaml:snakeyaml:1.11"
   def commonsCliVersion = "commons-cli:commons-cli:1.2"
   def commonsCodecVersion = "commons-codec:commons-codec:1.5"

   dependencies {
      compile "org.eclipse.jetty:jetty-server:${jettyVersion}"
      compile "${commonsCliVersion}"
      compile "${commonsCodecVersion}"
      compile "${snakeYamlVersion}"

      compile "org.eclipse.jetty:jetty-server:${jettyVersion}:sources"
      compile "org.eclipse.jetty:jetty-util:${jettyVersion}:sources"
      compile "${commonsCodecVersion}:sources"
      compile "${snakeYamlVersion}:sources"
      compile "${commonsCliVersion}:sources"

      testCompile('junit:junit:4.10') {
         transitive = false
      }
      testCompile 'org.mockito:mockito-all:1.9.0'
      testCompile 'com.google.http-client:google-http-client:1.12.0-beta'
      testCompile 'com.fasterxml.jackson.core:jackson-core:2.1.4'
      testCompile 'com.fasterxml.jackson.core:jackson-databind:2.1.4'
      testCompile 'com.fasterxml.jackson.core:jackson-annotations:2.1.4'
   }
